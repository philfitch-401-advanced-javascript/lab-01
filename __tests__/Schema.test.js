const Schema = require('../lib/Schema');

describe('Schema', () => {

  // add a test schema

  it('validates a correct model', () => {

  });

  it('throws on invalid model', () => {

  });

  // more test cases...
});